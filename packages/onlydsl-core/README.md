# onlydsl-core

Transport-neutral IFURI capability routing, Protobuf envelope/DSL documents,
CQRS primitives and runtime ports. The package does not implement NATS,
PostgreSQL, SQLite, filesystem artifact storage, LLM gateways, AQL or service
composition.
