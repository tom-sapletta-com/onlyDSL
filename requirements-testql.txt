# Minimal dependency set for TestQL's public verification API and API scenarios.
# Heavy optional GUI/LLM/autofix extras are deliberately absent from startup checks.
PyYAML>=6,<7
click>=8,<9
rich>=13,<16
httpx>=0.27,<1
